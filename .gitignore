# Environment / secrets
.env

# Composer — vendor ignored, composer.lock committed (reproducible deploys)
vendor/
composer.phar

# PHP cache / logs
*.log
*.cache
/tmp/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Claude Code project settings
.claude/

# deployment/ — never commit (db_pass in plaintext)
deployment/

# Ticker autocomplete list — admin panel edits this live on production
# (public/data/tickers.json.example is the seed for fresh clones/deploys).
public/data/tickers.json

# E2E (Playwright) — node_modules ignored, package-lock.json committed (reproducible installs)
node_modules/
test-results/
playwright-report/
blob-report/
playwright/.auth/
